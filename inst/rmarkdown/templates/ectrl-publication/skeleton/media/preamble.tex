% from EUROCONTROL brand identity page: https://www.eurocontrol.int/brand
%% Option 'sfdefault' only if the base font of the document is to be sans
\usepackage[sfdefault]{roboto}
%% for monospaced font
\usepackage{roboto-mono}
\usepackage[T1]{fontenc}

\usepackage{booktabs}
\usepackage{longtable}
\usepackage[bf,singlelinecheck=off]{caption}

% font for code
\usepackage[scale=.8]{sourcecodepro}

\usepackage{framed,color}
\definecolor{shadecolor}{RGB}{248,248,248}

% make floating environments less likely to float by allowing them
% to occupy larger fractions of pages without floating
\renewcommand{\textfraction}{0.05}
\renewcommand{\topfraction}{0.8}
\renewcommand{\bottomfraction}{0.8}
\renewcommand{\floatpagefraction}{0.75}

% linked to the use of 'krantz' class
\renewenvironment{quote}{\begin{VF}}{\end{VF}}

% redefine hyperlinks to be footnotes when the book is printed on paper
\let\oldhref\href
\renewcommand{\href}[2]{#2\footnote{\url{#1}}}

\ifxetex
  \usepackage{letltxmacro}
  \setlength{\XeTeXLinkMargin}{1pt}
  \LetLtxMacro\SavedIncludeGraphics\includegraphics
  \def\includegraphics#1#{% #1 catches optional stuff (star/opt. arg.)
    \IncludeGraphicsAux{#1}%
  }%
  \newcommand*{\IncludeGraphicsAux}[2]{%
    \XeTeXLinkBox{%
      \SavedIncludeGraphics#1{#2}%
    }%
  }%
\fi

% maybe useful to insert a cover page
\usepackage{pdfpages}

\usepackage{makeidx}
\makeindex

\urlstyle{tt}

\usepackage{amsthm}
\makeatletter
\def\thm@space@setup{%
  \thm@preskip=8pt plus 2pt minus 4pt
  \thm@postskip=\thm@preskip
}
\makeatother

\frontmatter
